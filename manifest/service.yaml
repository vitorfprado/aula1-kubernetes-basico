apiVersion: v1
kind: Service
metadata:
  name: exemplo-service
  namespace: aula-k8s
  labels:
    app: exemplo
spec:
  type: NodePort
  selector:
    app: exemplo
  ports:
    - name: http
      port: 80
      targetPort: 80
      nodePort: 30080
